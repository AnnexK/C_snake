#include <stdio.h>
#include "deque.h"
#include "game.h"

int main(void)
{
    //Game.init();

    //refresh();
    //wborder(Game.main.wptr, '#', '#', '#','#','#','#','#','#');
    Game.gamedata.S = malloc(sizeof(Snake));
    *(Game.gamedata.S) = createSnake();

    for (int i = 0; i < 3; i++)
    {
	//Game.gamedata.mv(Game.gamedata.S);
	//Game.main.display(&(Game.main), &(Game.gamedata));
	//Game.aux.display(&(Game.aux), &(Game.gamedata.score));
	//getch();
    }

    //Game.kill();
    return 0;
}
